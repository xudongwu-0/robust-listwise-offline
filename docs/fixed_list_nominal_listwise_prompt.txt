You must follow the mathematics in `docs/ROBUST_LISTWISE_DPO_MATH.md` exactly.

Read that file first and treat it as the source of truth.

## Current task
Implement fixed-list nominal listwise DPO training first.
Do not implement robust listwise yet.

## Required setting
- Use Qwen/Qwen2.5-0.5B-Instruct
- Use Hugging Face TRL as the training base
- Use LoRA
- Use 4-bit loading
- Use a fixed-list dataset built from `openbmb/UltraFeedback`
- For each prompt, keep K=4 candidate responses
- Derive the ranking label from `overall_score`

## Objective to implement
Use the DPO-style implicit score:
g_theta(x, y) = beta * log( pi_theta(y|x) / pi_ref(y|x) )

Then implement the nominal listwise Plackett–Luce loss exactly as defined in `docs/ROBUST_LISTWISE_DPO_MATH.md`.

Do not use a generic ranking loss.
Do not reduce the fixed-list data to pairwise chosen/rejected examples at this stage.

## Required outputs
Please create:

1. A fixed-list data loader / wrapper
2. A collator for K=4 candidate responses per prompt
3. A nominal listwise training script
4. A short README section with exact run commands
5. A short markdown note explaining:
   - how the fixed-list samples are built
   - how the score is computed
   - how the nominal PL loss is computed
   - what still needs to be done before robust listwise DPO

## Sanity checks
Before claiming success, explicitly verify:

1. The 4 responses in one sample come from the same prompt
2. Ranking labels are derived correctly from `overall_score`
3. Score direction is correct
4. K=2 nominal listwise behaves consistently with pairwise behavior
5. Small-batch training runs end-to-end
6. A checkpoint can be saved

## Important
- Do not implement robust listwise yet
- Do not add synthetic noise yet
- Do not expand to larger models
- Keep the first nominal listwise implementation minimal and debuggable

The goal of this stage is only:
make fixed-list nominal listwise DPO work end-to-end on a real LLM stack.
